# 1pm Afternoon Briefing

Run the following in order:

1. Check Gmail for any TLDR newsletters that arrived today. Read the key highlights
   in plain conversational language. If none, skip silently.

2. Send the briefing as a voice message using the notify voice command.

3. Immediately after the voice message, send the same text as a plain Telegram
   message using the notify send command (channels: "telegram"). This is the
   transcript — Daniel reads it to follow up in conversation.
