# Core
python-dotenv
pyyaml
requests
openai
setuptools<81
keyring
keyrings.alt

# MCP
mcp[cli]

# Browser automation
playwright

# Dev tooling
pip-audit

# Gateway server
fastapi
uvicorn[standard]
aiosqlite
aiohttp

# External integrations
neo4j
py2neo
dateparser

# Discord bot
discord.py[voice]>=2.0

# Telegram bot
python-telegram-bot[job-queue]>=21.0

# Scheduler
apscheduler>=3.10

# Voice server (STT + TTS)
faster-whisper>=1.0.0
soundfile
numpy
torch==2.6.0
torchaudio==2.6.0
# Chatterbox TTS deps (installed --no-deps in Dockerfile due to numpy<1.26 pin incompatible with Python 3.12)
librosa==0.11.0
s3tokenizer
transformers==4.46.3
diffusers==0.29.0
resemble-perth==1.0.1
conformer==0.3.2
safetensors==0.5.3
spacy-pkuseg
pykakasi==2.3.0
pyloudnorm
omegaconf
